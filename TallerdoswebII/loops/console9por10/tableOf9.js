// Función para imprimir la tabla del 9
function printTableOf9() {
    for (let i = 1; i <= 10; i++) {
        console.log(`9 x ${i} = ${9 * i}`);
    }
}

// Llamar a la función para imprimir la tabla del 9
printTableOf9();
